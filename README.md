# Kiku-
Whisper-architecture speech recognition in pure Rust — log-Mel frontend, encoder-decoder Transformer, KV-cached decoding with the temperature fallback ladder. Loads open Whisper checkpoints. No Python, no ffmpeg
